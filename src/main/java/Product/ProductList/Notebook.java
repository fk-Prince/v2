package Product.ProductList;

import Product.Entity.Product;

public class Notebook extends Product {
    public Notebook(int productId, String brandName, int productQty, double productPrice) {
        super(productId, brandName, productQty, productPrice);
    }
}
